"""Stage 6: Axl.ai analytics-service calculators.

Deterministic, audit-traceable financial calculators. Imported by main.py and
exercised by tests/. The LLM never calls these directly — the diligence
workflow POSTs structured inputs and we return a list of named results.
"""
